version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - sqlite-data:/app/sqlite
    container_name: timesnap-backend
    environment:
      - APP_MODE=prod

volumes:
  sqlite-data:
    name: timesnap_sqlite_data
